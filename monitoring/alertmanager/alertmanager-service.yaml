apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: monitoring
  labels:
    app: alertmanager
spec:
  type: NodePort
  ports:
    - name: web
      port: 9093
      targetPort: 9093
      nodePort: 30093
  selector:
    app: alertmanager
